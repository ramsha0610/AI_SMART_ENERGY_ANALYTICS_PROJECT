# Streamlit configuration for AI Smart Energy Analytics.
# Light enterprise theme — matches utils.visualization / config.Theme.

[theme]
base = "light"
primaryColor = "#4F6BED"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F9FAFB"
textColor = "#111827"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
